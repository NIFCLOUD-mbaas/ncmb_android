name: AndroidSDK_ReferenceCI
on:
  push:
    branches:
      - feature/ci_generate_reference
      # 最終的にmasterに変更する

jobs:
  generate_reference:
    runs-on: macOS-latest
    steps:
      - uses: actions/checkout@v2
      - name: set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
      - name: generate reference
        run: npm run document:generate
