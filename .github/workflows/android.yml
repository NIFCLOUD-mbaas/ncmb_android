name: Android

on: [push, pull_request]

env:
  MAVEN_USERNAME: ${{ secrets.OSSRH_USERNAME }}
  MAVEN_PASSWORD: ${{ secrets.OSSRH_TOKEN }}

jobs:
  build_and_test:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: set up JDK 1.8
      uses: actions/setup-java@v1
      with:
        java-version: 1.8
        server-id: ossrh
        server-username: ${{ secrets.OSSRH_USERNAME }}
        server-password: ${{ secrets.OSSRH_TOKEN }}
        gpg-private-key: ${{ secrets.SIGNING_GNUPG_SECRETKEY }}
        gpg-passphrase: ${{ secrets.SIGNING_GNUPG_PASSWORD }}
    - name: Publish to Nexus repository
      uses: sonatype-nexus-community/nexus-repo-github-action@master
      with:
        serverUrl: https://s01.oss.sonatype.org/service/local/staging/deploy/maven2/
        username: ncmbsdk
        password: MAVEN_PASSWORD
        format: maven2
        repository: maven-release
        coordinates: groupId=com.nifcloud.mbaas artifactId=ncmb_android version=4.1.0
        assets: extension=jar
        filename: ././././